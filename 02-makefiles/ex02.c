#include <stdio.h>

/* a very basic C program... */
int main(int argc, char *argv[]) {
    // another comment...
    int age = 40;
    printf("Tim, now you are %i years old!\n", age);
    return 0;
}
